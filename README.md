Angular JS Application skeleton
======

All steps to initialize the Angular JS application

1. Install/update libraries via Bower

=> bower install  or bower update (if libraries are already installed)

